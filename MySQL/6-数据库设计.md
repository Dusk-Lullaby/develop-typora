# 数据库设计

## 1. 设计数据库

### 1.1 什么是实体？

实体就是软件开发过程中所涉及的事物，通常都是一类数据对象的个体

### 1.2 什么是数据库设计？

数据库设计就是实体与实体之间的关系进行规划和结构化的过程

### 1.3 为什么要对数据库进行设计

当存储的数据比较少的时候，当然不需要对数据库进行设计。但是当数据的需求越来越大时，对数据库的设计就很有必要性了。如果数据库的设计不当，会造成数据冗余、修改复杂、操作数据异常等问题。好的数据库设计则可以减少不必要的数据冗余，通过合理的数据规划提高系统的性能。

### 1.4 如何设计数据库？

<font color = "blue">收集信息</font>

在确定客户要做什么之后，收集一切相关的信息，尽量不遗漏任何信息

<font color = "blue">标识实体</font>

实体一般是名词，每个实体只描述一件事情，不能重复出现相同含义的实体

<font color = "blue">标识实体的详细属性</font>

标识每个实体需要存储的详细信息

<font color = "blue">标识实体之间的关系</font>

理清实体与实体之间的关系



## 2. ER图

### 2.1 什么是ER图

ER = Entity Relational （实体关系）

ER图就是实体关系图

### 2.2 如何绘制ER图

矩形标识实体，一般是名词

椭圆形表示实体属性，一般是名词

菱形表示实体关系，一般是动词

<font color = "blue">示例</font>

![](image\ER图示例.png)



## 3. 数据库模型图

### 3.1 什么是关系模式

实体关系的描述称为关系模式，关系模式通常使用二维表的形式表示

<font color = "blue">示例</font>

学生（学号，姓名，性别，年龄，所属班级）

班级（班级编号，班级名称）

### 3.2 关系模式转为数据库模型图

将关系模式使用`Navicat`工具转换为数据库的模型图，转换步骤如下：

* 将各实体转换为对于的表，将各属性转换为各表对应的列
* 标识每个表的主键列
* 在表之间建立主外键，体现实体

<font color = "blue">示例</font>

![数据库模型图](image\数据库模型图.png)



## 4. 数据库三大范式

### 4.1 第一范式

第一范式是最基本的范式，确保每列保持原子性，也就是每列不可再分

<font color = "blue">示例</font>

![第一范式](image\第一范式示例.png)

### 4.2 第二范式

第二范式是在第一范式的基础上，每张表的属性完全依赖于主键，也就是每张表只描述一件事情

<font color = "blue">示例</font>

![第二范式](D:\Typora\develop\MySQL\image\第二范式示例.png)

### 4.3 第三范式

第三范式是在第二范式的基础上，确保每列都直接依赖于主键，而不是间接依赖于主键，也就是不能存在传递依赖。比如A依赖B，B依赖C，这样A间接依赖C

<font color = "blue">示例</font>

![第三范式示例](image\第三范式示例.png)

<font color = "blue">练习</font>

假设某建筑公司要设计一个数据库，公司的业务规则概括说明如下：

* 公司承担多个工程项目，每一个工程有：工程号、工程名称、工程人员等
* 公司有多名职工，每一名职工有：职工号、姓名、性别、职务（工程师、技术员）等
* x 1package com.lullaby.layer.util;2​3import java.lang.reflect.Field;4import java.lang.reflect.Method;5import java.lang.reflect.Modifier;6import java.sql.*;7import java.util.ArrayList;8import java.util.List;9​10/**11 * JDBC 数据库操作工具类12 * 封装了数据库连接的获取、资源释放以及通用的增删改查（CRUD）操作。13 */14public class JdbcUtil {15​16    // 数据库连接配置信息17    private static String url = "jdbc:mysql://localhost:3306/lesson?serverTimezone=Asia/Shanghai";18    private static String username = "root";19    private static String password = "root";20​21    // 静态代码块：类加载时执行一次，用于注册 MySQL 驱动22    static {23        try {24            Class.forName("com.mysql.cj.jdbc.Driver");25        } catch (ClassNotFoundException e) {26            throw new RuntimeException("数据库驱动加载失败", e);27        }28    }29​30    /**31     * 获取数据库连接32     *33     * @return Connection 数据库连接对象34     */35    private static Connection getConnection() {36        try {37            return DriverManager.getConnection(url, username, password);38        } catch (SQLException e) {39            throw new RuntimeException("获取数据库连接失败", e);40        }41    }42​43    /**44     * 安全关闭数据库资源45     * 接收可变参数，适用于关闭 ResultSet, PreparedStatement, Connection 等实现了 AutoCloseable 接口的对象46     *47     * @param autoCloseables 待关闭的资源数组48     */49    private static void close(AutoCloseable... autoCloseables) {50        if (autoCloseables != null && autoCloseables.length > 0) {51            for (AutoCloseable autoCloseable : autoCloseables) {52                // 增加非空判断，防止空指针异常53                if (autoCloseable != null) {54                    try {55                        autoCloseable.close();56                    } catch (Exception e) {57                        // 仅打印堆栈信息，禁止向上抛出异常，防止阻断后续其他资源的关闭58                        e.printStackTrace();59                    }60                }61            }62        }63    }64​65    /**66     * 构建并初始化 PreparedStatement67     *68     * @param sql        包含占位符（?）的 SQL 语句69     * @param params     替换占位符的具体参数数组70     * @param connection 数据库连接对象71     * @return 预编译的 PreparedStatement 对象72     * @throws SQLException 数据库访问异常73     */74    private static PreparedStatement getPreparedStatement(String sql, Object[] params, Connection connection) throws SQLException {75        PreparedStatement preparedStatement = connection.prepareStatement(sql);76        // 动态填充 SQL 占位符参数77        if (params != null && params.length > 0) {78            for (int i = 0; i < params.length; i++) {79                // JDBC 参数索引从 1 开始80                preparedStatement.setObject(i + 1, params[i]);81            }82        }83        return preparedStatement;84    }85​86    /**87     * 万能查询方法（支持动态对象映射）88     *89     * @param sql    查询的 SQL 语句90     * @param clazz  映射的目标实体类的 Class 对象91     * @param params 替换占位符的参数92     * @param <T>    泛型，代表实体类类型93     * @return 包含实体对象的 List 集合94     */95    public static <T> List<T> query(String sql, Class<T> clazz, Object... params) {96        List<T> dataList = new ArrayList<>();97        Connection connection = null;98        PreparedStatement preparedStatement = null;99        ResultSet resultSet = null;100        try {101            connection = getConnection();102            preparedStatement = getPreparedStatement(sql, params, connection);103            resultSet = preparedStatement.executeQuery();104​105            // 遍历结果集中的每一行记录106            while (resultSet.next()) {107                // 通过反射调用无参构造函数，实例化目标对象108                T instance = clazz.getDeclaredConstructor().newInstance();109                // 获取该实体类中声明的所有字段（包含私有属性）110                Field[] fields = clazz.getDeclaredFields();111​112                for (Field field : fields) {113                    // 性能优化：过滤掉静态字段和常量（如 serialVersionUID），避免无意义的底层异常触发114                    if (Modifier.isStatic(field.getModifiers()) || Modifier.isFinal(field.getModifiers())) {115                        continue;116                    }117​118                    String fieldName = field.getName();119                    try {120                        // 尝试从结果集中获取与属性同名的列值（利用 JDBC 4.1 的泛型获取机制转换类型）121                        Object value = resultSet.getObject(fieldName, field.getType());122​123                        // 若数据库中该字段值为非空，则通过 Setter 方法注入对象124                        if (value != null) {125                            // 按照 Java Bean 规范，拼接 Setter 方法名 (例如: name -> setName)126                            String methodName = "set" + fieldName.substring(0, 1).toUpperCase() + fieldName.substring(1);127                            // 获取对应的 Setter 方法实例128                            Method method = clazz.getDeclaredMethod(methodName, field.getType());129                            // 执行方法完成赋值130                            method.invoke(instance, value);131                        }132                    } catch (SQLException | NoSuchMethodException e) {133                        // 预期内异常：SQL 结果集中没有该列，或实体类中没有对应的 Setter 方法134                        // 属于正常现象，静默跳过，继续处理下一个字段135                        continue;136                    } catch (Exception e) {137                        // 预期外异常（如安全管理器拦截、反射执行异常），记录日志供排查138                        e.printStackTrace();139                    }140                }141                // 将组装好的对象加入集合142                dataList.add(instance);143            }144            return dataList;145        } catch (Exception e) {146            throw new RuntimeException("执行查询操作失败，SQL：" + sql, e);147        } finally {148            // 确保资源必然被释放149            close(resultSet, preparedStatement, connection);150        }151    }152​153    /**154     * 万能更新方法（涵盖 INSERT、UPDATE、DELETE）155     *156     * @param sql    执行更新的 SQL 语句157     * @param params 替换占位符的参数158     * @return 数据库受影响的行数159     */160    public static int update(String sql, Object... params) {161        Connection connection = null;162        PreparedStatement preparedStatement = null;163        try {164            connection = getConnection();165            preparedStatement = getPreparedStatement(sql, params, connection);166            // 执行并返回受影响的行数167            return preparedStatement.executeUpdate();168        } catch (SQLException e) {169            throw new RuntimeException("执行更新操作失败，SQL：" + sql, e);170        } finally {171            // 确保发生异常时连接也能被释放，防止连接池泄漏172            close(preparedStatement, connection);173        }174    }175}java

<font color = "blue">分析</font>

1. 找出实体
2. 找出实体关系
3. 绘制ER图，然后将ER图转换为数据库模型图
4. 使用三大范式规范数据库设计

![ER图](image\MySQL三大范式练习.png)

![](D:\Typora\develop\MySQL\image\数据库模型练习图.png)